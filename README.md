# run local

```
sudo gem install bundler:2.0.1
bundle add webrick
bundle install
bundle exec jekyll serve --watch
```

#### it will show
```
Configuration file: <project directory>/_config.yml
 Theme Config file: <project directory>/_config.yml
            Source: <project directory>
       Destination: <project directory>/_site
 Incremental build: disabled. Enable with --incremental
      Generating... 
                    done in 0.093 seconds.
 Auto-regeneration: enabled for '<project directory>'
    Server address: http://127.0.0.1:4000/
  Server running... press ctrl-c to stop.
  ```